Practical No – 6
Aim : Write a program to implement an RSA algorithm to perform encryption/decryption of a given String
Code :
from math import gcd

def RSA(p: int, q: int, message: int):
    n = p * q
    t = (p - 1) * (q - 1)
    for i in range(2, t):
        if gcd(i, t) == 1:
            e = i
    j = 0
    while True:
        if (j * e) % t == 1:
            d = j
            break
        j += 1
    ct = (message ** e) % n
    print(f"Encrypted message is {ct}")
    mes = (ct ** d) % n
    print(f"Decrypted message is {mes}")                         

p=int(input("Enter value of p : "))
q=int(input("Enter value of q : "))
msg=int(input("Enter value of msg : "))
RSA(p,q,msg)


Output:

